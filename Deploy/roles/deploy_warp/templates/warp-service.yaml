apiVersion: v1
kind: Service
metadata:
  name: warp
  namespace: {{ namespace }}
  labels:
    app: warp
spec:
  ports:
  - port: 8200
    targetPort: 8200
    protocol: TCP
    name: http
  selector:
    app: warp